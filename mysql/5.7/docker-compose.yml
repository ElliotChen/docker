version: "3.3"

services:
  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: abcd1234
    restart: always
    ports:
      - 3306:3306
    volumes:
      - ./mysql:/var/lib/mysql